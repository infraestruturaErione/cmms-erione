gzip on;
gzip_comp_level 5;
gzip_min_length 1024;
gzip_proxied any;
gzip_vary on;
gzip_types
  application/javascript
  application/json
  application/manifest+json
  application/rss+xml
  application/vnd.ms-fontobject
  application/wasm
  application/x-font-ttf
  application/xml
  font/otf
  font/ttf
  image/svg+xml
  text/css
  text/javascript
  text/plain
  text/xml;

server {
  listen 3000;
  client_max_body_size 50m;

  root /usr/share/nginx/html;
  index index.html index.htm;

  location = /api {
    return 308 /api/;
  }

  location ^~ /api/ {
    rewrite ^/api/(.*)$ /$1 break;
    proxy_pass http://api:8080;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    # CONTRATO DE IP DO CLIENTE (nao mudar sem ler): o backend (ClientIpResolver) conta o X-Forwarded-For DA
    # DIREITA (TRUSTED_PROXY_HOPS). Este nginx e' um proxy que ACRESCENTA: o ultimo item e' o par TCP que ele
    # viu ($remote_addr); tudo a esquerda pode ser texto do cliente e e' ignorado pelo backend. Por isso o
    # $proxy_add_x_forwarded_for (append) e' o correto aqui - trocar por $remote_addr faria o backend enxergar
    # so' o IP da borda quando houver OpenResty/LB na frente (todos os clientes num unico balde).
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  location ^~ /atlas-bucket/ {
    proxy_pass http://minio:9000/atlas-bucket/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=86400" always;
  }

  location = /index.html {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    try_files $uri =404;
  }

  location = /runtime-env.js {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    try_files $uri =404;
  }

  location ~* \.(?:js|css|woff2?|ttf|otf|eot|ico|png|jpg|jpeg|gif|svg|webp|avif)$ {
    add_header Cache-Control "public, max-age=31536000, immutable" always;
    try_files $uri =404;
  }

  location / {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    try_files $uri $uri/ /index.html =404;
  }
}
